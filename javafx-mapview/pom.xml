<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <artifactId>javafx-mapview</artifactId>
    <packaging>jar</packaging>
    <name>MapView based on the JavaFX WebView</name>
    <parent>
        <groupId>slash.navigation</groupId>
        <artifactId>RouteConverter</artifactId>
        <version>2.15</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <dependencies>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>common</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>mapview</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>navigation-formats</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <!--suppress MavenModelInspection -->
            <groupId>com.oracle</groupId>
            <!--suppress MavenModelInspection -->
            <artifactId>javafx</artifactId>
            <!--suppress MavenModelInspection -->
            <version>2.0</version>
            <!--suppress MavenModelInspection -->
            <!-- JavaFX path on Windows -->
            <systemPath>${java.home}/lib/jfxrt.jar</systemPath>
            <!-- JavaFX path on Mac OS X -->
            <!-- <systemPath>${java.home}/lib/ext/jfxrt.jar</systemPath> -->
            <scope>system</scope>
        </dependency>
    </dependencies>
</project>
